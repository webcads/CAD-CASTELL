---
import LogoCAD from "../logo-CAD.astro";
import { Image } from "astro:assets";
import pathImageBranding from  "../../imgs/branding.svg"
import pathImgLogotipo from "../../imgs/logotipo.svg"
import pathImgAsistant from "../../imgs/asistant.png"
import Branding from '../resources/Branding.astro'
import LogoTipo from '../resources/LogoTipo.astro'
import CAD_Asistant from '../resources/CAD_Asistant.astro'
// const  themeColor  = '#676767';
const zoomResolution = '1'; 
const isDark = false;
const themeColor = isDark ? '#ffffff' : '#676767';
---
<script is:inline>import { transitionEnabledOnThisPage } from "astro:transitions/client";
    
    const element = document.documentElement;
    element.classList.toggle("dark");
    isDark = element.classList.contains("dark");
    // localStorage.setItem("theme", isDark ? "dark" : "light");     
    //    alert(isDark)   ;
    // Asigna un valor apropiado a themeColor
    // const themeColor = isDark ? 'white' : 'black';
    // alert(themeColor)
</script>

<style>
    button{
        /* background: #0062a7; */
        transition: all ease 0.3s
    }
    
</style>


<header id="header-hero" class="min-h-screen flex flex-col  justify-center align-middle   p-10 md:p-20  text-center rounderd-xl    md:min-h-fit" style="align-items: center; padding-top: 150px; ">
    <!-- brading -->
    <div class="branding flex-1 text-center flex flex-col  " style="align-items: center; max-width: 300px; "   >   
        
        <div class="flex flex-col  gap-1">
            <span class="pr-1" style="width: 100%; display: flex; justify-content: center;  ">                
                <Branding brandingColor={"cyan"}  w="100px" ></Branding>
            </span>  
            <span class="text-center" style="max-width: fit-content; position: relative; display:block; min-width: 240px;">                
                <LogoTipo brandingColor={"cyan"}   w="300px"></LogoTipo>    
            </span>   
            
        </div>
        
        
        <span class="flex pt-10 pb-10 flex-col " style=" max-width: 400px; width: 300px;">
            
            <button type="button" class="py-2.5 px-5 me-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-full border border-gray-200  hover:text-white focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 hover:bg-cyan-500 ">Webs</button>  
            <button type="button" class="py-2.5 px-5 me-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-full border border-gray-200  hover:text-white focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 hover:bg-cyan-500 ">Design</button>
            
            <button type="button" class="py-2.5 px-5 me-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-full border border-gray-200  hover:text-white focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 hover:bg-cyan-500 ">Ads</button>
            
        </span>  
        
        
        <span class="flex" style="    padding-left: 80%;
        padding-right: 1%;
        display: block;
        min-width: 100%;
        }">
            <CAD_Asistant color={"cyan"} w="150px"></CAD_Asistant>
        </span>        



</div>

<!-- Asistant -->
<!-- <span class="flex " style="display: flex ; align-items: end;">
    <CAD_Asistant color={"cyan"}   w="150px" ></CAD_Asistant>
</span> -->



</header>

<script is:inline>
    function toggleDarkMode() {
        const elementT = document.documentElement;
        const isDarkMode = elementT.classList.contains("dark");
        
        // Asegúrate de realizar las acciones necesarias según el valor de isDarkMode
        const themeColor = isDarkMode ? '#ffffff' : '#676767';        
        // Actualiza el tema del LogoCAD
        const logoCAD = document.querySelector('LogoCAD');
        if (logoCAD) {
            logoCAD.setAttribute('theme', themeColor);
        }
        // alert(isDarkMode+themeColor);
    }
    
    // Ejecutar cuando se carga la página
    document.addEventListener('DOMContentLoaded', toggleDarkMode);
    
    // Ejecutar cuando cambia el tema
    document.addEventListener('theme-change', toggleDarkMode);
</script>
